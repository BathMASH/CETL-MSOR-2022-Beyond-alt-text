# How to help

Although not yet implimented the procedure to convert the resources is as follows:

- browse list of images to convert in a shared spreadsheet
- add resources of converted images to spreadsheet
- run script that places objects into the correct location

```{r echo= FALSE}
csv_data = read.csv("csv/list_of_images.csv")
ka


```
